# Core Data Science & ML
numpy >= 1.23.0, < 2.0.0
pandas >= 1.5.0, < 3.0.0
scipy >= 1.9.0, < 2.0.0
matplotlib >= 3.6.0, < 4.0.0
seaborn >= 0.12.0
scikit-learn >= 1.2.0, < 1.6.0

# Advanced ML Models
xgboost >= 1.7.0
lightgbm >= 3.3.0
catboost >= 1.2.0

# Visualization
plotly >= 5.14.0

# MLOps & Experiment Tracking
mlflow >= 3.3.0
dvc == 3.63.0
optuna >= 3.2.0

# Jupyter & Development
jupyter >= 1.0.0
jupyterlab >= 4.0.0
ipywidgets >= 8.0.0

# Utilities
pyyaml >= 6.0
python-dotenv >= 1.0.0
tqdm >= 4.65.0
joblib >= 1.3.0

# Time Series (for energy prediction)
statsmodels >= 0.14.0
pmdarima >= 2.0.0

# Data Quality & Validation
great-expectations >= 0.17.0
evidently >= 0.7.14

# Testing
pytest >= 8.4.0
pytest-cov >= 4.1.0
pytest-mock >= 3.0.0

# Linting & Formatting
black >= 23.0.0
flake8 >= 6.0.0
isort >= 5.12.0
ruff >= 0.1.0

# Other MLOps dependencies
cookiecutter-data-science == 2.3.0
fastapi == 0.116.1
sdv == 1.26.0
deepchecks == 0.19.1
onnxruntime == 1.22.1
dvc_gdrive == 3.0.1
dagshub
